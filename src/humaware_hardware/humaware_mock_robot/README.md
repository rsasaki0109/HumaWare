# humaware_mock_robot

Stability: experimental.

This package publishes mock humanoid runtime state for launch, diagnostics, tooling, and CI validation. It is not a simulator and does not implement physics.
