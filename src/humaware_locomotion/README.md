# Locomotion Packages

HumaWare locomotion packages define the boundary between approved humanoid movement requests and robot-specific walking adapters.

Initial packages:

- `humaware_locomotion_interface`
- `humaware_mock_locomotion_adapter`
