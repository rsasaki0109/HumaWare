# Teleop Packages

HumaWare teleop packages provide operator command sources. They publish candidate commands only; they do not publish approved hardware commands.

Initial packages:

- `humaware_keyboard_teleop`
