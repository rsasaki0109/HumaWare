# Run Real Robot

Real robot support must be explicit and verified per robot model.

Before running on hardware, record:

- robot model
- firmware version
- ROS distro
- network setup
- battery state
- surface and environment
- git SHA
- launch command

Do not run real hardware commands through unreviewed demo scripts. Use a verified hardware adapter and route commands through safety management.
